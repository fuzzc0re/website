import React from 'react';
import TransitionedView from '../containers/TransitionedView';

const PageNotFound = () => (
  <TransitionedView>
    <div>Sorry but the requested URL could not be found!</div>
  </TransitionedView>
);

export default PageNotFound;
